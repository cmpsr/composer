const theme = require('./src/styles/theme.js');
const variants = require('./src/styles/variants.js');
const plugins = require('./src/styles/plugins.js');

module.exports = {
  theme,
  variants,
  plugins,
};
