import { ReactElement } from "react";
import { DocumentNode } from "graphql";
declare type Props = {
    id: string;
    query: DocumentNode;
    name: string;
};
export declare const getContent: <T>({ id, query, name }: Props) => ReactElement;
export {};
//# sourceMappingURL=index.d.ts.map