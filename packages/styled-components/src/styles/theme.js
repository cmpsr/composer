import colors from './colors';

const theme = {
  colors,
  Button: {
    borderRadius: {
      rounded: '1.75rem',
      semiRounded: '0.5rem',
      rectangle: '0'
    }
  }
};

export default theme;
